# =============================================================================
# PDFQA Pipeline — Python Dependencies
# =============================================================================
# Install with:  pip install -r requirements.txt
#
# System-level prerequisites (install BEFORE pip):
#   macOS   : brew install tesseract poppler libmagic
#   Ubuntu  : sudo apt-get install tesseract-ocr poppler-utils libmagic1
#   Windows : choco install tesseract poppler
# =============================================================================

# --- PDF Processing --------------------------------------------------------
pdfplumber>=0.11.0
PyMuPDF>=1.24.0

# --- OCR -------------------------------------------------------------------
pytesseract>=0.3.10
pdf2image>=1.17.0

# --- Image Processing ------------------------------------------------------
opencv-python>=4.10.0
Pillow>=10.0.0

# --- File Validation -------------------------------------------------------
python-magic>=0.4.27

# --- Virus Scanning (optional — graceful skip if ClamAV not installed) -----
pyclamd>=0.4.0

# --- Networking / Download --------------------------------------------------
requests>=2.32.0

# --- Progress Bars ----------------------------------------------------------
tqdm>=4.66.0

# --- Testing ----------------------------------------------------------------
pytest>=8.0.0
